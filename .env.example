# Copy this file to `.env` for local development.
# `.env` is gitignored — it must never be committed.
# In GitHub Actions, the same variable names are set under repo Settings → Secrets and variables → Actions.

# === Toast ===
# Generate at: Toast Web → Integrations → API Access → Standard API Access
TOAST_KIZUKI_CLIENT_ID=
TOAST_KIZUKI_CLIENT_SECRET=
TOAST_SD_CLIENT_ID=
TOAST_SD_CLIENT_SECRET=

# === Stripe (QR code donations) ===
# Restricted API key from the Stripe account hosting the QR Payment Link.
# Dashboard → Developers → API keys → Create restricted key.
# Permission needed: "Checkout sessions: Read" (only).
STRIPE_API_KEY=

# The Payment Link ID for the QR code donation link (starts with "plink_").
# Find it in Dashboard → Payment Links → click your link → URL bar (after /payment-links/).
STRIPE_PAYMENT_LINK_ID=

# === Shopify (frozen dumpling sales) ===
# Dev Dashboard app credentials. We exchange CLIENT_ID + CLIENT_SECRET for a 24-hour
# Admin API access token via OAuth client_credentials grant each run.
# Find these at: https://dev.shopify.com/dashboard → your app → Settings → Credentials
# The app needs `read_orders` scope and must be installed on the store.
SHOPIFY_CLIENT_ID=
SHOPIFY_CLIENT_SECRET=
SHOPIFY_STORE_DOMAIN=          # e.g. supreme-dumplings-ec.myshopify.com
